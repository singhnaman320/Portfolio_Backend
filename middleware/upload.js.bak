// This file has been moved to .bak as it's no longer needed
// File upload functionality has been removed in favor of static files
// Original upload.js content moved here for reference if needed

const multer = require('multer');
const path = require('path');
const fs = require('fs');

// This middleware is no longer used
// Static files are now served from frontend public directory
